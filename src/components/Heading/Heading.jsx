

const Heading = ({Heading}) => {
    return (
        <div className="text-3xl hover:text-[aqua] font-bold text-center mb-10">
            {Heading}
        </div>
    );
};

export default Heading;